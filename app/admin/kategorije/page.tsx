import { saveCategory } from "@/features/admin/actions";
import { requireAdmin } from "@/lib/security/admin";
export const dynamic="force-dynamic";
export default async function Page(){const{supabase}=await requireAdmin(["owner","manager"]);const{data}=await supabase.from("categories").select("id,name,slug,is_active,position").order("position");const input="min-h-11 rounded-xl border border-neutral-200 px-3";return <main className="p-5 sm:p-8"><p className="text-sm font-semibold text-neutral-500">Redosled i vidljivost</p><h1 className="text-4xl font-bold tracking-[-.05em]">Kategorije</h1><div className="mt-7 grid gap-4">{(data??[]).map(category=><form key={category.id} action={saveCategory} className="grid gap-3 rounded-3xl bg-white p-5 sm:grid-cols-[50px_1fr_1fr_auto] sm:items-end"><input type="hidden" name="id" value={category.id}/><div><label className="text-xs font-bold text-neutral-400">Red</label><div className="mt-2 grid size-11 place-items-center rounded-xl bg-neutral-100 font-bold">{category.position+1}</div></div><div><label className="text-sm font-bold">Naziv</label><input name="name" required defaultValue={category.name} className={`mt-2 w-full ${input}`}/></div><div><label className="text-sm font-bold">Slug</label><input name="slug" required defaultValue={category.slug} className={`mt-2 w-full ${input}`}/></div><div className="flex items-center gap-3"><label className="flex min-h-11 items-center gap-2 font-bold"><input name="isActive" type="checkbox" defaultChecked={category.is_active} className="size-5 accent-black"/>Aktivna</label><button className="min-h-11 rounded-full bg-black px-5 font-bold text-white">Sačuvaj</button></div></form>)}</div></main>;}
