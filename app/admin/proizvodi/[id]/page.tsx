import{notFound}from"next/navigation";import{z}from"zod";import{ProductForm}from"@/components/admin/product-form";import{requireAdmin}from"@/lib/security/admin";import type{Category,Product}from"@/types/domain";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;if(!z.string().uuid().safeParse(id).success)notFound();const{supabase}=await requireAdmin(["owner","manager"]);const[{data:product},{data:categories}]=await Promise.all([supabase.from("products").select("id,category_id,name,slug,description,ingredients,price,image_url,accent_color,contains_alcohol,preparation_minutes,max_quantity_per_order,position,is_available,is_active").eq("id",id).single(),supabase.from("categories").select("id,name,slug,image_url,position,is_active").order("position")]);if(!product)notFound();return <main className="p-5 sm:p-8"><h1 className="text-4xl font-bold tracking-[-.05em]">Izmeni {product.name}</h1><ProductForm product={product as Product} categories={(categories??[]) as Category[]}/></main>;}
