export function StatCard({ label, value, detail }: { label: string; value: string | number; detail?: string }) { return <div className="min-w-0 rounded-2xl bg-white p-4 shadow-[0_8px_30px_rgba(0,0,0,.035)] sm:rounded-3xl sm:p-5"><p className="text-xs font-semibold text-neutral-500 sm:text-sm">{label}</p><p className="mt-2 break-words text-2xl font-bold tracking-[-.05em] sm:mt-3 sm:text-3xl">{value}</p>{detail ? <p className="mt-1 text-xs text-neutral-400">{detail}</p> : null}</div>; }
