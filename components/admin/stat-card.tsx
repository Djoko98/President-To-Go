export function StatCard({ label, value, detail }: { label: string; value: string | number; detail?: string }) { return <div className="rounded-3xl bg-white p-5 shadow-[0_8px_30px_rgba(0,0,0,.035)]"><p className="text-sm font-semibold text-neutral-500">{label}</p><p className="mt-3 text-3xl font-bold tracking-[-.05em]">{value}</p>{detail ? <p className="mt-1 text-xs text-neutral-400">{detail}</p> : null}</div>; }
