"use client";
import { useState } from "react";
import { ImageUp, LoaderCircle } from "lucide-react";
import { toast } from "sonner";
import { createClient } from "@/lib/supabase/browser";

const allowed=["image/png","image/webp","image/jpeg"];
export function ProductUpload({productId}:{productId:string}){const[progress,setProgress]=useState(0);async function upload(event:React.ChangeEvent<HTMLInputElement>){const file=event.target.files?.[0];if(!file)return;if(!allowed.includes(file.type)){toast.error("Dozvoljeni su PNG, WebP i JPEG.");return;}if(file.size>5*1024*1024){toast.error("Slika može imati najviše 5 MB.");return;}setProgress(15);const extension=file.name.split(".").pop()?.toLowerCase()||"png";const path=`products/${productId}/${crypto.randomUUID()}.${extension}`;const supabase=createClient();setProgress(40);const{error}=await supabase.storage.from("product-images").upload(path,file,{contentType:file.type,upsert:false,cacheControl:"31536000"});if(error){toast.error(error.message);setProgress(0);return;}setProgress(80);const{data}=supabase.storage.from("product-images").getPublicUrl(path);const update=await supabase.from("products").update({image_url:data.publicUrl}).eq("id",productId);if(update.error){toast.error(update.error.message);setProgress(0);return;}setProgress(100);toast.success("Slika proizvoda je postavljena.");location.reload();}return <div><label className="inline-flex min-h-12 w-full cursor-pointer items-center justify-center gap-2 rounded-full border border-neutral-300 px-5 font-bold sm:w-auto"><input type="file" accept="image/png,image/webp,image/jpeg" className="sr-only" onChange={upload} disabled={progress>0&&progress<100}/>{progress>0&&progress<100?<LoaderCircle className="animate-spin" size={18}/>:<ImageUp size={18}/>}Postavi sliku</label>{progress>0?<div className="mt-2 h-2 overflow-hidden rounded-full bg-neutral-200"><div className="h-full bg-black transition-all" style={{width:`${progress}%`}}/></div>:null}</div>;}
